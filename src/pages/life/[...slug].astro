---
import PostPage from "../../components/PostPage.astro";
import { getPostStaticPaths } from "../../lib/posts";
import type { PostPageProps } from "../../lib/posts";

export async function getStaticPaths() {
  return getPostStaticPaths("life");
}

const props = Astro.props as PostPageProps;
---

<PostPage {...props} />
